en:
  activerecord:
    errors:
      models:
        auth_token:
          attributes:
            expires_at:
              in_past: "must be after %{now}"
        user_preference:
          attributes:
            min_rating:
              inclusion: "value not between %{min} and %{max}, must have step of %{step}"
  responses:
    errors:
      invalid_credentials: 'Invalid credentials'
